generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Products {
  id          String   @id @default(uuid())
  name        String
  price       Float
  status      String
  photo       String?
  description String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @default(now())
  Orders      Orders[]
}

model Orders {
  id          String   @id @default(uuid())
  productsId  String
  client      String
  status      String
  observation String?
  userChange  String   @unique
  createdAt   DateTime @default(now())
  updatedAt   DateTime @default(now())
  Products    Products @relation(fields: [productsId], references: [id])
  User        Users    @relation(fields: [userChange], references: [id])
}

model Users {
  id        String   @id @default(uuid())
  name      String
  username  String
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())
  Orders    Orders?
}
